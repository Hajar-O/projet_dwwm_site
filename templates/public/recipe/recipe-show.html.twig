{% extends 'admin/base.admin.html.twig' %}

{% block content %}

    <main>
        <div>
            <h3>{{ recipe.title }}</h3>
            <p>{{ recipe.category }}</p>
            <p>{{ recipe.time }}</p>
            <p>{{ recipe.description }}</p>
        </div>
       
    </main>

{% endblock %}