{% extends 'admin/base.admin.html.twig' %}

{% block content %}
 <main>
     {{ form_start(recipeForm) }}

         {{ form_row(recipeForm.title) }}
         {{ form_row(recipeForm.image) }}
         {{ form_row(recipeForm.idCategory) }}
     {{ form_end(recipeForm) }}
     <label>Ingrédients</label>
     {# pour chaque famille d'ingrédient #}
     <details>
         <summary>{{Famille.name}}</summary>
         {# pour chaque ingrédient dans la famille #}
             <input  type="checkbox"> {{ ingredient.name}} <input type="number">
             <select name="volume" id="">
                 <option value="litre">L</option>
                 <option value="gramme">g</option>

             </select>
     </details>
     <input  type="text">
     {{ form_start(recipeForm) }}

         {{ form_row(recipeForm.description) }}

     {{ form_end(recipeForm) }}
 </main>



{% endblock %}