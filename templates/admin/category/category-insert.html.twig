{% extends 'admin/base.admin.html.twig' %}
{% block content %}
        <section>
            {{ form_start(categoryForm) }}
                {{ form_row(categoryForm.type) }}
            {{ form_end(categoryForm) }}
        </section>
{% endblock %}