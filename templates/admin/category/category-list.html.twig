{% extends 'admin/base.admin.html.twig' %}

{% block content %}

    <section>
        {% for categorie in categories %}

            <div>
                <h3>{{ categorie.type }}</h3>
                <p><a href="{{ path('admin_category_delete',{id: categorie.id}) }}">Supprimer</a></p>
                <p><a href="{{ path('admin_category_update',{id: categorie.id}) }}">Modifer</a></p>
            </div>


        {% endfor %}
    </section>
{% endblock %}